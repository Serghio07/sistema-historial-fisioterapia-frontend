import test from'node:test';import assert from'node:assert/strict';import{monitoringBadge,safePollMs}from'../src/utils/whatsappMonitoringUi.js';test('estado general usa estilos administrativos',()=>{assert.match(monitoringBadge('OPERATIVO'),/emerald/);assert.match(monitoringBadge('ERROR_CONFIGURACION'),/red/)});test('polling se limita entre 15 y 300 segundos',()=>{assert.equal(safePollMs(1),15000);assert.equal(safePollMs(60),60000);assert.equal(safePollMs(999),300000)});
